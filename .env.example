# ===========================================
# Lighthouse Design Build - Environment Config
# ===========================================
# Copy this file to .env and fill in your values

# -------------------------------------------
# Server Configuration
# -------------------------------------------
NODE_ENV=production
PORT=3000

# Session secret (generate a secure random string)
# Use: openssl rand -base64 32
SESSION_SECRET=your-secure-random-string-here

# -------------------------------------------
# HubSpot Blog Integration
# -------------------------------------------
HUBSPOT_RSS_URL=https://blog.raymonddesignbuilders.com/news-info/rss.xml

# -------------------------------------------
# Storage Configuration
# -------------------------------------------
# Options: 'local' (default), 'r2', or 'railway'
STORAGE_TYPE=railway

# -------------------------------------------
# Cloudflare R2 Configuration
# (Required when STORAGE_TYPE=r2)
# -------------------------------------------
# Get these from Cloudflare Dashboard -> R2 -> Manage R2 API Tokens

# Your Cloudflare account ID (found in dashboard URL or R2 overview)
R2_ACCOUNT_ID=your-account-id

# R2 API Token credentials (create with Object Read & Write permissions)
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key

# R2 bucket name
R2_BUCKET=lighthouse-media

# Public URL for your R2 bucket
# Option 1: R2.dev subdomain (enable in R2 bucket settings)
#   Example: https://pub-xxxxx.r2.dev
# Option 2: Custom domain (configure in Cloudflare DNS)
#   Example: https://cdn.yourdomain.com
R2_PUBLIC_BASE_URL=https://your-r2-public-url.r2.dev

# -------------------------------------------
# Railway Buckets Configuration
# (Required when STORAGE_TYPE=railway)
# -------------------------------------------
# Get these from Railway Dashboard -> Project -> Volumes -> Object Storage

# Railway bucket name
RAILWAY_BUCKET_NAME=lighthouse-media

# S3-compatible endpoint from Railway
# Example: https://your-project.railway.app or the S3 endpoint URL
RAILWAY_BUCKET_ENDPOINT=https://your-bucket-endpoint.railway.app

# Railway bucket credentials (from Railway dashboard)
RAILWAY_ACCESS_KEY_ID=your-access-key-id
RAILWAY_SECRET_ACCESS_KEY=your-secret-access-key

# Region (optional, defaults to us-east-1)
RAILWAY_BUCKET_REGION=us-east-1

# Presigned URL expiry in seconds (default: 604800 = 7 days)
MEDIA_PRESIGN_EXPIRY_SECONDS=604800

# Media path prefixes (optional)
MEDIA_BASE_PATH_IMAGES=images
MEDIA_BASE_PATH_VIDEOS=videos

# -------------------------------------------
# Platform Detection (auto-set by platforms)
# -------------------------------------------
# VERCEL=1  (set automatically by Vercel)
# RAILWAY_ENVIRONMENT=production  (set automatically by Railway)
